# Copy this file to "keystore.properties" (same folder) and fill in real values.
# keystore.properties is git-ignored — never commit it or the .keystore file.
#
# storeFile is resolved relative to the android/ folder.
storeFile=../jobtracker-release.keystore
storePassword=CHANGE_ME
keyAlias=jobtracker
keyPassword=CHANGE_ME
